# Rust / Cargo
/target
**/target
Cargo.lock.bak

# Tauri
/src-tauri/target
/src-tauri/gen

# Node / frontend
node_modules
dist
.vite
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Local env / secrets
.env
.env.local
*.local

# Claude local settings (per-user, not shared)
.claude/settings.local.json

# Test/scratch PDFs (large)
/test-fixtures/*.pdf
/scratch

# Benchmark corpus — large real PDFs copied from the server, NEVER commit
/bench/corpus/

# PDFium prebuilt binary — downloaded, not committed
/src-tauri/resources/libpdfium.dylib
/src-tauri/resources/libpdfium.so
/src-tauri/resources/pdfium.dll

# Benchmark rendered output (PNG proofs)
/bench/results/*.png
