# Rust / Cargo
/target
**/target
Cargo.lock.bak

# Tauri
/src-tauri/target
/src-tauri/gen

# Node / frontend
node_modules
dist
.vite
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Local env / secrets
.env
.env.local
*.local

# Claude local settings (per-user, not shared)
.claude/settings.local.json

# Test/scratch PDFs (large)
/test-fixtures/*.pdf
/scratch

# Benchmark corpus — large real PDFs copied from the server, NEVER commit
/bench/corpus/

# PDFium prebuilt binary — downloaded, not committed
/src-tauri/resources/libpdfium.dylib
/src-tauri/resources/libpdfium.so
/src-tauri/resources/pdfium.dll

# OCR tessdata (eng.traineddata, ~3.9 MB) — downloaded via
# scripts/fetch-ocr-tessdata.sh, not committed. Stale reference to Phase 1's
# `.rten` ocrs models removed 2026-09-03 (Phase 2a switched engines to
# Tesseract/leptess — see docs/ocr.md).
/src-tauri/resources/ocr/

# Benchmark rendered output (PNG proofs, incl. nested dirs like render-matrix/)
/bench/results/*.png
/bench/results/**/*.png

# WDIO E2E run logs (docs/TESTING.md Tier 2)
/logs/
